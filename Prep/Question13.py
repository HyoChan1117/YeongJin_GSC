# 사용자로부터 여러 문장으로 구성된 하나의 문자열을 입력 받아,
# 문자열에서 사용자가 지정하 단어가 몇 번 등장하는지를 카운트 하는 프로그램 작성

# 여러 문장으로 구성된 하나의 문자열을 입력 받는다.
check = input("문자열 입력: ").split()
frequency = input("단어 입력: ")
print(len(check))

#for _ in range(len(check)):
#    if check

# 결과를 출력한다.
