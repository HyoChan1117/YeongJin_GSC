# def 함수명(매개변수):   # 함수인지 아닌
#     함수 호출 시 실행 명령어

# 함수 정의(function definetion) : -> 1번
def print_name(name):
    print(name, "님 안녕하세요")
    
# 함수 호출(function call) : -> 2번
print_name("리차드")
print_name("정영철")

# 함수의 이름을 지어주는 이유 : 
# 기능별로 나눠서 함수로 나눠서 이름을 지어주면
# 나중에 재사용 가능하고 유지 보수가 편하기 때문