# from typing import Sequence

from typing import Sequence

# Sequence는 순서를 가지는 list, tuple, range를 가리킨다.
x: Sequence = [1, 2, 3, 4]
x = {"value1": 1, "value2": 2, "value3":3, "value4": 4}
x = (1, 2, 3, 4)
x = range(5)
